using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GameManager : MonoBehaviour
{
    //private static GameManager instance;    // Only one version of this variable and no copies can be made of it
    /*public static GameManager Instance;
    

    public bool _isGameOver;
    

    
    // determine when a game is over

    // restart the game 


    void Awake()
    {
        // Set instance to this, and do a nullcheck
        if (Instance == null)   // if there is no instance already
        {
            Debug.LogError("Game Manager is Null!");
            Instance = this;
        }
        else if (Instance != this)  // if there is already an instance and its not this instance
        {
            // Instance = this;
            Destroy(gameObject);
        }

        
    }



    public void GameOver(bool flag)
    {
        _isGameOver = flag;
    }

    public bool IsGameOver()
    {
        return _isGameOver;
    }*/

}
